[package]
name = "bridge"
version = "0.1.0"
edition = "2021"
description = "SupportFlow Agent bridge/ — channel context, bot routing, AgentBridge (Python bridge/ port)"

[dependencies]
agent = { path = "../agent" }
models = { path = "../models" }
serde_json = "1"
tracing = "0.1"
dirs = "6"

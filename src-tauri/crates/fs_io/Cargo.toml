[package]
name = "fs_io"
version = "0.1.0"
edition = "2021"
description = "Audited std::fs wrappers with structured logging (shared infrastructure)"

[lints]
workspace = true

[dependencies]
tracing = "0.1"

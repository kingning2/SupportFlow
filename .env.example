# Agent 工作区（可选，仅影响 skills/memory/mcp 等文件目录，不决定 config 来源）。
# 配置固定从 src-tauri/resources/config.json 读取。
# SUPPORT_FLOW_WORKSPACE=D:/Desktop/SupportFlow
SUPPORT_FLOW_WORKSPACE=

# 通道 sidecar exe（默认 src-tauri/binaries/cowagent-channels-<target>.exe）
# COW_CHANNEL_SIDECAR_EXE=D:/path/to/cowagent-channels.exe
# 开发模式 python -m channel（wework 须 3.8–3.10）。先运行: bun run setup:channel-sidecar-dev
# COW_PYTHON_EXECUTABLE=C:\Users\...\Python38\python.exe
# 打包 sidecar 时用的 Python（默认可自动选 py -3.8）：
# COW_SIDECAR_PYTHON=C:\Users\...\Python38\python.exe
